[package]
name = "project"
version = "0.1.0"
authors = ["efugier <mail@emilienfugier.net>"]
edition = "2018"

[dependencies]
termion = {version = "1.5", optional = true }
unicode-width = "0.1.5"
tui = { version = "0.5.0", features = ["termion"] }

[features]
default = ["termion"]